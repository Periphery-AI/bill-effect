# Opencode backend

This folder is reserved for the Opencode backend. Add Opencode here as a git submodule or clone.

Submodule example:
```bash
git submodule add https://github.com/opencode-ai/opencode apps/opencode
```
